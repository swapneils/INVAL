;; #!/usr/local/bin/ecl -shell

;; Note: load is only requried when used interactively or as a script.
;; (load "inval.lsp")
;; (load "rsk.lsp")
;; (load "simplify.lsp")
;; (load "fdr3.lsp")

;; Note: See this file for some additional things you
;; might need to un-comment
(load "nyat-main-internal.lsp")

(run-nyat)
